# Data Notes

This project uses the UCI Student Performance dataset loaded programmatically using the `ucimlrepo` package.

Dataset source:
- UCI Machine Learning Repository
- Dataset name: Student Performance

Because the dataset is downloaded directly in code, no raw CSV file is stored in this folder by default.
